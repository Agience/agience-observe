type: grant
namespace: agience
slug: user-reads-all-tools
principal: "{{user.id}}"
resource: agience/agience-seeds-all-tools
actions: [read]
name: Platform seed collection (auto-granted on first login)
